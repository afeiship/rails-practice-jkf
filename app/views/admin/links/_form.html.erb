<%= form_for(admin_link) do |f| %>
  <% if admin_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_link.errors.count, "error") %> prohibited this admin_link from being saved:</h2>

      <ul>
      <% admin_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>


    <% if f.object.image? %>
        <%= image_tag f.object.image.thumb.url %>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :link %>
    <%= f.text_field :link %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
